# Server-side only. Never prefix with NEXT_PUBLIC_.
ALCHEMY_API_KEY=your_alchemy_api_key

# Optional protection for /admin until a real identity provider is connected.
ADMIN_ACCESS_TOKEN=replace_with_a_long_random_value
